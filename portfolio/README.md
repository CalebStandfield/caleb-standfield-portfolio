# portfolio

React + Vite + TypeScript app for Caleb Standfield's portfolio. See the
[root README](../README.md) for the full overview and repository layout.

```bash
npm install
npm run dev      # dev server at http://localhost:5173
npm run build    # typecheck + production build to dist/
npm run preview  # serve the production build
npm run lint     # eslint
```
